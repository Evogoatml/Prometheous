"""
Prometheous tools — utility actions the orchestrator can dispatch to.

Each tool exposes a run() or main() function. The system can wire these
into the orchestrator as agents (or call them directly from a worker).
"""
